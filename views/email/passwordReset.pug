extends baseemail 

block content
    //- what is that below
    p Hi #{firstname}, 
        p Forgot your password? Submit a PATCH request with your new password and passwordConfirma to: #{url}.
        p (website for this action pending implementation)
        table.btn.btn-primary(role='presentation', border='0', cellpadding='0', cellspacing='0')
            tbody
                tr
                    td(align='left')
                        table(role='presentation', border='0', cellpadding='0', cellspacing='0')
                            tbody
                                tr
                                    td
                                        a(href= `${url}`, target='_blank') Reset your passwords
        p If  u did not forget your password, please ignore this email
        
